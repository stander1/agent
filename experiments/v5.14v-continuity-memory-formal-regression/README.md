# v5.14v Continuity and Memory Formal Regression

This formal run places the v5.14u mechanism back into the unchanged real MiMo
Provider matrix:

- A1-A10 and B1-B10;
- Native, Observed, and Managed groups;
- the same capability-based Agent configurations;
- temperature 0, nine-turn maximum, anonymous quality judges, and all v5.14t
  cost, quality, delivery, reliability, memory, state, and protocol gates.

Additional evidence records:

- compact approvals that resolve to a validated prior artifact and produce a
  non-rejected typed-memory reference;
- observed current-task identity guard events and whether each event has a
  grounded task sequence and an explicit unsupported assertion.

Zero identity-guard events is valid when the model does not drift. The run fails
only for malformed guard events or for missing approved-artifact memory
resolution despite approval-bearing collaboration.
