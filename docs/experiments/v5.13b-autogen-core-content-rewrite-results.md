# v5.13b AutoGen Core Content Rewrite Results

## 结论

`v5.13b` 在 AutoGen Core Runtime 层新增了类型保持的真实改写能力。

含义是：

```text
保持原 Python 消息类型不变；
只替换 content / body / text 等明确文本字段；
长文本正文进入 StatePool；
底层传输字段变成 SHP state_ref wire packet + receiver Prompt View。
```

这一步比 v5.13a 的 shadow 接管更进一步：`send_message` 和 `publish_message` 不只是被记录，而是在安全开关开启时发生了真实字段替换。

## 验证命令

```text
F:\software\anaconda\envs\agentlite-autogen\python.exe .\examples\run_autogen_core_content_rewrite_smoke.py --output-dir .\runs\v5.13b-autogen-core-rewrite-20260702-001
```

运行环境变量：

```text
AGENTLITE_AUTOGEN_BROADCAST_MODE=real-rewrite
AGENTLITE_AUTOGEN_CORE_CONTENT_REWRITE=1
```

## 关键结果

```text
passed: true
driver_phase_v5_13b: true
core_content_rewrite_enabled: true
direct_and_publish_received: true
native_type_preserved_at_receiver: true
payload_kind_preserved_at_receiver: true
rewrite_marker_received: true
content_shortened_at_receiver: true
core_rewrite_events_recorded: true
send_and_publish_rewritten: true
rewrite_applied_without_fallback: true
shadow_still_recorded: true
state_refs_recorded: true
schema_valid: true
prompt_view_available: true
token_reduced: true
```

核心 trace 指标：

```text
autogen_core_content_real_rewrite: 2
rewrite_applied_count: 2
rewrite_fallback_count: 0
methods: publish_message, send_message
state_ref_count: 2
schema_valid_count: 2
prompt_view_count: 2
token_delta_native_minus_rewrite: 2158
```

接收方证据：

```text
received message type: CorePayload
payload_kind: core_transport_state
direct content chars: 3420 -> 1291
publish content chars: 3926 -> 1289
agentlite_rewrite_marker: true
```

这证明真实改写没有破坏 AutoGen Core 的 Python 类型分发，接收方 handler 仍能匹配 `CorePayload`。

## 当前能力边界

可以表述为：

```text
AgentLite 已经能在 AutoGen Core Runtime 的 send_message / publish_message 入口，
对含有 content / body / text 文本字段的消息做类型保持真实改写。
```

仍不能表述为：

```text
已经能无条件替换所有任意自定义 Core 消息；
已经自动接管 AutoGen Studio / 网页端后端进程；
已经覆盖分布式 runtime、远程 runtime、跨进程 transport 和所有第三方 AutoGen 封装。
```

原因是任意对象并不一定有可替换文本字段；部分业务 handler 也可能要求读取原始正文而不是 Prompt View。后续要继续推进 receiver 侧 Prompt View 透明解析和 Studio 后端进程注入。
