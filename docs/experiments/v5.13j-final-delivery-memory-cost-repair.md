# v5.13j 最终交付、共享记忆与成本口径修复

日期：2026-07-17

## 修复目标

本次修复针对 A1-A10 连续任务实验暴露出的三条连锁问题：

1. Reviewer 的审查意见只要附带 `FINAL_ANSWER_READY` 就会被误判为完整交付；
2. 被误判的审查意见会以高置信度进入长期共享记忆；
3. 影子压缩估算、真实改写成本和 Provider Token 被混在同一节省结论中。

这些问题属于通用 Contract Guard、规则优先记忆准入和 CSCC 成本闸门的工程缺口，不新增创新方案。Context-Pruned Retry 只处理格式或协议损坏，不负责修正答案内容质量。

## 已完成修复

| 问题 | 修复后行为 |
|---|---|
| 只检查 Reviewer 和末行标记 | 标记只生成最终候选；规则语义闸门继续检查正文是否为可直接交付的产物。 |
| 审查意见被当作交付 | 运行结果原样保留；离线通用守卫标记为任务失败，不追加轮次。 |
| 达到最大回合仍不完整 | 记录 `delivery_status=task_failed`、`delivery_valid=false`，不调用额外语义修复。 |
| Team 最终消息全部入长期记忆 | 先经过最终交付校验；不合格输出进入候选池后由规则拒绝，正文仍保留在 StatePool 供审计。 |
| 领域槽位影响公平性 | 删除 Question A/B 专用槽位注入；正式实验只使用通用记忆机制。 |
| 命中即算有效 | 拆分为命中、注入、有效采用、错误命中和待评估；没有下游证据时有效采用保持为 0。 |
| 有记忆引用就绕过成本闸门 | 删除绕过逻辑；短消息加上 Prompt View 和记忆扇出后更贵时保留原生消息。 |
| 影子估算算入真实收益 | `actual_*` 只统计真实改写或真实回退，`shadow_*` 只表示理论潜力。 |

## 最终交付状态

- `validated_final_artifact`：规则校验通过，可作为用户交付物和长期记忆候选。
- `task_failed`：达到相同轮次上限后，输出为空、过短或仍是审查意见；原始输出仍完整保存。
- `semantic_retry_count`：正式公平实验固定为 0；内容质量失败不会触发额外 LLM 调用。

Question A 的三个候选、逐日安排、预算和连续性要求只进入统一盲评评分表，不注入运行时。

## 记忆指标口径

- `memory_hit_count`：检索器返回的记忆条数。
- `memory_injected_count`：实际进入 Prompt View 的记忆条数。
- `useful_memory_hit_count`：存在下游采用证据的记忆条数；不能再由命中数自动赋值。
- `wrong_memory_hit_count`：被判定为错误、过时或无关的记忆条数。
- `unassessed_memory_hit_count`：已经注入，但尚无有效或错误证据的记忆条数。
- `unique_retrieved_memory_tokens`：每个检索视图只计一次。
- `fanout_retrieved_memory_tokens`：向多个 Agent 展开后的总读取量。

旧版 Trace 没有注入和待评估字段。重新解析旧日志时，系统会保留可确认的“已注入”事实，但把旧的 `useful=hit` 降级为 `unassessed`。

## 成本指标口径

- `actual_native_transport_tokens`：真实改写审计覆盖范围内的原生传输成本。
- `actual_agentlite_transport_tokens`：真实应用改写后的成本；若改写回退，则按原生成本计入。
- `actual_transport_token_savings_ratio`：上述两者的真实差额比例。
- `shadow_potential_savings_ratio`：未实际替换消息的影子方案理论潜力。
- `llm_total_tokens`：Provider 返回的真实模型输入和输出 Token，必须单独展示。

使用新解析器重放 2026-07-17 接管组旧 Trace 后：

| 指标 | 复算结果 |
|---|---:|
| 真实原生传输基准 | 206,986 |
| 真实 AgentLite 传输成本 | 218,840 |
| 真实传输变化 | +5.73% |
| 影子原生候选成本 | 638,981 |
| 影子压缩候选成本 | 110,559 |
| 影子理论节省 | 82.70% |
| 记忆命中 / 注入 / 已证明有效 / 待评估 | 9 / 9 / 0 / 9 |

这说明旧报告的约 82% 只应解释为理论压缩潜力，不能解释为真实端到端收益。

## 验证结果

```text
F:\software\anaconda\envs\agentlite-autogen\python.exe -m unittest discover -s tests -p 'test_*.py'
Ran 159 tests
OK
```

另外已通过 `compileall` 和 `git diff --check`。

### 2026-07-18 P0 工程补齐

本轮曾使用已取回的 `v5.13j-20260717-175115` 接管组 A1-A10 输出进行离线诊断，没有调用 LLM。下表只记录守卫误判的发现过程；领域边界校准后，这些题目要求全部退出运行时，只能由统一盲评评分表评价：

| 任务 | 旧判断 | 新判断 | 校准原因 |
|---|---|---|---|
| A1 | 通过 | 拒绝 | `ReviewerAgent 审查意见` 只是修订清单，不是最终交付物。 |
| A2 | 拒绝 | 通过 | `目的地1/2/3` 是合法的三个结构化候选。 |
| A3-A5 | 通过 | 通过 | 保持原判断，并从 A3 提取已确认目的地。 |
| A6 | 拒绝 | 通过 | `10点后出发` 满足首日十点以后出发约束。 |
| A7 | 通过 | 通过 | 保持原判断。 |
| A8 | 通过 | 拒绝 | 输出把莫干山静默改为皖南/浙西，破坏连续任务事实。 |
| A9-A10 | 通过 | 通过 | 输出继续包含已确认的莫干山目的地。 |

同时补齐了两条运行时链路：

1. Team 启动时把参与 Agent 绑定到同一协作组；Agent 输入检索不再落入彼此隔离的 `agent_<id>` 记忆域。
2. Agent 输入真实改写统一比较“原始完整输入”与“状态 Prompt View + Memory View + 控制信封”的 Token；即使命中记忆，也不能绕过 `token_not_reduced` 成本闸门。

新增集成测试证明：长上下文下共享 Memory View 能进入 Agent 输入；短上下文下若注入后更贵，则保留原生消息；纯审查意见不能晋升为长期 MemoryObject。

### 2026-07-18 领域边界校准

复核发现，旧核心还内置了 A10/B10 最终任务判断、旅游/安全交付 Schema，以及 `travel_preference/security_audit` 槽位分支。这些硬编码及后续尝试引入的 Delivery Policy、benchmark Profile 已全部退出正式运行路径。

Question A 团队保留旅游专精 Planner、Writer、Reviewer，但三组统一使用 AutoGen 标准终止条件和 6 turn。达到上限仍不合格时直接记录失败，不追加语义修复。

## 下一轮实验门槛

1. 先定向运行 A1、A2、A3、A8、A9，确认三组 Agent、轮次、终止条件和模型参数完全一致。
2. 检查不合格 Team 输出的记忆候选为 `rejected`，合格 FinalArtifact 才能进入 MemoryObject。
3. 完整运行原生、观察、接管三组，每组至少 3 次。
4. 先冻结盲评，再解盲比较 Provider Token、质量、真实传输成本与影子潜力。
5. 只有质量和语义交付不下降时，Provider Token 降幅才可作为比赛结论。

## 尚未由本次修复证明的事项

- 通用规则测试通过不等于真实 MiMo A1-A10 已恢复质量，仍需统一盲评。
- `useful_memory_hit_count` 现在不再虚增，但要获得正值仍需下游引用或 Reviewer 采用证据。
- AutoGen Core 尚有未支持的消息内容字段；对应真实 Core 输出改写成功率需要单独继续提升。
- 正式实验不执行内容质量语义修复；网络和协议重试仍必须完整计入 Provider Token。
