# v5.13e AutoGen Core Message Matrix Results

## 结论

`v5.13e` 回归验证了 AutoGen Core 消息结构矩阵。

覆盖对象：

```text
dataclass + content
dataclass + body
dataclass + text
pydantic BaseModel + content
publish_message 广播路径 + content
```

每条消息都完成：

```text
发送端类型保持真实改写；
StatePool 写入；
SHP state_ref wire 传输；
接收端 BaseAgent.on_message 自动 Prompt View hydration；
用户 handler 仍收到原 Python 消息类型；
AgentLite wire marker 不泄漏给用户 handler。
```

## 验证命令

```text
F:\software\anaconda\envs\agentlite-autogen\python.exe .\examples\run_autogen_core_message_matrix_smoke.py --output-dir .\runs\v5.13e-autogen-core-matrix-20260702-001
```

运行环境变量：

```text
AGENTLITE_AUTOGEN_BROADCAST_MODE=real-rewrite
AGENTLITE_AUTOGEN_CORE_CONTENT_REWRITE=1
AGENTLITE_AUTOGEN_CORE_RECEIVER_HYDRATE=prompt-view
```

## 关键结果

```text
passed: true
driver_phase_v5_13e: true
all_cases_received: true
direct_and_publish_received: true
expected_fields_received: true
expected_types_received: true
rewrite_events_for_all_messages: true
hydration_events_for_all_messages: true
rewrite_applied_without_fallback: true
hydration_applied_without_fallback: true
wire_marker_not_leaked: true
prompt_view_received: true
content_shortened_at_receiver: true
token_reduced: true
```

矩阵覆盖：

```text
received_cases:
  dataclass_body
  dataclass_content
  dataclass_content_publish
  dataclass_text
  pydantic_content

received_types:
  MatrixBodyPayload
  MatrixContentPayload
  MatrixPydanticPayload
  MatrixTextPayload

received_fields:
  body
  content
  text
```

核心 trace 指标：

```text
autogen_core_content_real_rewrite: 5
autogen_core_receiver_hydration: 5
rewrite_applied_count: 5
rewrite_fallback_count: 0
hydration_applied_count: 5
hydration_fallback_count: 0
autogen_core_transport_shadow: 5
token_delta_native_minus_rewrite: 2844
```

边界说明：

```text
真实 AutoGen Core handler 不支持裸 dict 作为 message handler 类型；
本地验证会报 ValueError: No serializers found for type <class 'dict'>。
因此 dict / namedtuple / mutable object 放在 helper 单测中覆盖字段识别和同类型克隆。
```
