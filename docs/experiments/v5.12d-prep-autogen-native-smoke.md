# v5.12d-prep AutoGen 原生样例准备记录

## 当前状态

本轮没有安装 AutoGen 依赖，只完成真实 AutoGen 样例和校验脚本准备。

当前环境检查：

```text
autogen_agentchat: False
autogen_core: False
autogen_ext: False
autogen: False
```

因此正式 `v5.12d` smoke 尚未运行。

## 新增文件

```text
examples/autogen_native_roundrobin_smoke.py
examples/run_autogen_native_smoke.py
docs/planning/v5.12d-prep-真实AutoGen原生样例验证准备.md
```

## 设计选择

样例使用真实 AutoGen 包中的：

```text
UserProxyAgent
RoundRobinGroupChat
TextMentionTermination
TextMessage
CancellationToken
```

选择 `UserProxyAgent` 的原因是它不需要外部 LLM API key，可以把本轮验证聚焦到：

```text
AutoGen 生命周期能否被 AgentLite Driver 透明观测；
trace 是否记录 receive / output；
StatePool 是否产生 artifact_state；
原生 AutoGen 返回值是否保持不变。
```

## 安装后验证命令

```powershell
python -m pip install -e ".[autogen]"
python .\examples\run_autogen_native_smoke.py
```

## 安装前验证

编译检查：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -m compileall -q agent_runtime examples tests
```

结果：通过。

全量单元测试：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -m unittest discover -s tests -v
```

结果：

```text
Ran 98 tests
OK
```

缺依赖路径：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe .\examples\run_autogen_native_smoke.py --output-dir runs\v5.12d-prep-missing-deps-smoke
```

输出：

```text
Missing AutoGen modules: autogen_agentchat, autogen_core
Install after confirmation with: python -m pip install -e ".[autogen]"
```

返回码：`2`。

## 尚未完成

```text
真实 AutoGen 包未安装；
v5.12d smoke 未执行；
未产生真实 AutoGen 的 autogen_native_smoke_report.json；
未验证 AutoGen Studio 网页端。
```
