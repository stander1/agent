# 工程校验：二次审计缺失项修复记录 v5.9

生成日期：2026-06-22

对照文件：`C:/Users/千梦/Desktop/架构合规性审计报告v2.md`

## 修复口径

本轮不处理 AutoGen 接入和 openEuler 部署。原因是用户已明确要求先把运行时内核做完整，再接主流 Agent 框架。

本轮采用比赛版 Lite 实现：每个审计缺口必须有明确代码接口、规则路径和测试证据；不把未来研究增强版说成已经完整实现。

## 缺失项处理结果

| 审计编号 | 问题 | 本轮处理 | 证据 |
|---|---|---|---|
| R-01 | 7 态生命周期自动转换 | 已补 Memory 和 State 的 `dormant/outdated` 自动转换。Memory 支持 `apply_lifecycle_transitions()`，State 支持 `apply_lifecycle_transitions()` 和 `supersede_state()`。 | `agent_runtime/memory/memory_store.py`、`agent_runtime/state/state_pool.py`、`tests/test_memory_governance.py`、`tests/test_state_lifecycle.py` |
| R-02 | 9 层 GC 完善 L7-L9 | 已补 Lite 版 L7/L8/L9：retry-carried 保护、DAG/lineage/dependency 保护、raw view cache I/O budget GC。 | `StateGCReport.retry_carried_protected_count`、`dag_liveness_protected_count`、`raw_cache_evicted_count` |
| R-03 | Cold Access Manager 完善 | 已补 Raw Chunk Index、Span Resolver、Prefetch Scheduler、Materialized Raw View Cache 的 Lite 接口。 | `build_raw_chunk_index()`、`resolve_raw_span()`、`prefetch_raw_view()`、`request_cold_access()` |
| R-04 | 跨任务端到端收益验证 | 已补连续任务实验入口和自动报告导出，并完成真实 MiMo A->B 共享 runtime 实验。20/20 对 20/20 任务全部成功；`runtime_lite` 的 LLM token 下降 97.25%，端到端协作 token 下降 98.88%，延迟下降 18.74%。 | `examples/run_cross_task_eval.py`、`examples/run_cross_task_local_eval.py`、`tests/test_cross_task_eval.py`、`docs/experiments/v5.9-mimo-cross-task-results.md` |
| R-05 | Soft Deprecation + Compensating Events | 已补 `soft_deprecate()` 和 `CompensatingEvent`，替代硬回滚。 | `compensating_events()`、`render_storage_view()` |
| R-06 | View Layering 缺 Storage View | 已补独立 `render_storage_view()`，与 Prompt/Audit View 分离。 | `MemoryStoreLite.render_storage_view()` |
| R-07 | CSCC 完整版 | 已补 Lite 版 Section Dependency Map、Write-Intent Fence、Patch Regeneration 记录。 | `open_write_intent()`、`validate_write_intent()`、`record_patch_regeneration()` |
| R-08 | 期望成本模型 | 已补 `E(summary-first) = C_s + p*(C_req+C_r+C_re)` 与 raw-first 对比字段。 | `AccessEscalationReport.summary_first_expected_cost`、`raw_first_expected_cost`、`decision_basis` |
| R-09 | Layered Memory 持久化 | 已补 Hot/Warm/Cold Lite：Hot 为内存对象，Warm 为 SQLite `memory_records`，Cold 为 filesystem JSON。 | `layered_storage_report()`、`memory_warm.sqlite`、`memory_cold/*.json` |
| R-10 | Loop Budget | 已补 `LoopBudgetConfig`、`LoopBudgetReport`，retry summary 会按预算裁剪 attempts 和 carried states。 | `assess_loop_budget()`、`write_retry_loop_summary()` |
| R-11 | Cold Start Tie Resolver | 已补显式 tie-break 接口。 | `CapabilityRouterLite.resolve_tie()` |
| R-12 | LLM Budget 三级控制 | 已补 rules-first / scoring-assist / llm-fallback / blocked 控制路径字段。 | `ControlBudgetReport.control_path` |

## 本轮新增验证

新增或扩展测试覆盖：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -m unittest tests.test_memory_governance tests.test_state_lifecycle tests.test_communication_governance
```

通过情况：

```text
Ran 25 tests in 0.266s
OK
```

后续又补充了跨任务报告生成测试：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -m unittest tests.test_cross_task_eval
```

已补本地无 API smoke 实验：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -u examples\run_cross_task_local_eval.py --suite A --suite B --rounds 1 --mode both --max-tasks-per-suite 1 --output-dir runs\v5.9-cross-task-local-smoke
```

关键结果见 `docs/experiments/v5.9-cross-task-local-smoke-results.md`：

- A+B `end_to_end_collaboration_tokens`: `67,992 -> 9,008`，下降 `86.75%`。
- B 组 `memory_hit_count=2`，`useful_memory_hit_count=2`。
- `wrong_memory_hit_count=0`，`raw_access_count=0`。

随后完成真实 MiMo 跨任务正式实验：

```powershell
F:\software\anaconda\envs\multi-agent-demo\python.exe -u examples\run_cross_task_eval.py --suite A --suite B --rounds 1 --mode both --config configs\llm.mimo.example.json --max-retries 4 --retry-backoff-seconds 3 --output-dir runs\v5.9-mimo-cross-task-20260622-final2
```

关键结果见 `docs/experiments/v5.9-mimo-cross-task-results.md`：

- A+B `llm_total_tokens`: `9,126,251 -> 251,228`，下降 `97.25%`。
- A+B `end_to_end_collaboration_tokens`: `13,285,501 -> 148,501`，下降 `98.88%`。
- A+B `latency_ms`: `2,155,834 -> 1,751,797`，下降 `18.74%`。
- `useful_memory_hit_count=44`，`wrong_memory_hit_count=0`。
- `fallback_count=0`，`raw_access_count=0`。
- A10 最终 schema `16/16`，B10 最终 schema `12/12`。

## 仍需补强的证据项

跨任务真实成本数据已经完成，不再缺少 provider 实测。当前剩余的是语义质量审计：`run_cross_task_eval.py` 尚未持久化 baseline 最终正文，因此本轮只能证明成功率、字段完整度和协议质量，不能在结束后补做 baseline/runtime 的独立 LLM 语义裁判。

后续应先增加最终交付正文持久化，再执行独立质量裁判。该补强不影响本轮 R-04 的 token、延迟、记忆命中和 raw access 结论。

## 当前结论

二次审计指出的非 AutoGen 内核缺口已经从“无接口/无证据”推进到“Lite 机制可调用、具备测试并有真实 MiMo 跨任务成本证据”。更准确的表述是：

> 当前系统已补齐二次审计中除框架适配和独立语义质量裁判外的主要运行时内核缺口；R-04 的真实跨任务 token、延迟、记忆复用和冷访问证据已经完成。
